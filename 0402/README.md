# Linux System Study

## 학습 내용

### 시스템 호출과 파일

- 시스템 호출(System Call)
- C 라이브러리 함수
- 파일의 개념
- 텍스트 파일과 이진 파일

---

### 파일 입출력

- `fopen()`
- `fclose()`
- `fgetc()`
- `fputc()`
- `fgets()`
- `fputs()`
- `fprintf()`
- `fscanf()`

---

### 파일 관리

- `FILE` 구조체
- 파일 디스크립터(File Descriptor)
- 표준 입출력 스트림
- 파일 위치 제어 (`fseek`, `ftell`, `rewind`)

---

### 이진 파일 처리

- `fread()`
- `fwrite()`
- 구조체 저장 및 읽기

---

### 기타

- 입출력 버퍼(Buffer)
- 대소문자 변환 프로그램

---
